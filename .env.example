# Upstream API credentials (kept on host only)
INTERCOM_TOKEN=
OPENAI_API_KEY=

# Per-agent proxy tokens
# Generate with e.g. `openssl rand -hex 24`
AGENT_SUPPORT_TOKEN=
AGENT_SALES_TOKEN=
AGENT_RESEARCH_TOKEN=

# Proxy binding (localhost-only by default)
VAULT_BIND=127.0.0.1
VAULT_PORT=8282
